เปิดโฟลเดอร์ใน VS code ใช้โฟลเดอร์นี้เป็น workspace แล้วรันโค้ดด้านล่างนี้เลยเพื่อน โค้ดอันนี้เอาไว้โหลดพวก mediapipe กับอย่างอื่นที่จำเป็น

pip install -r requirements.txt


--------------------------------------------------------------------------------------------

เสร็จแล้วให้กดรัน infer_cam.py ไฟล์นี้เอาไว้รันโปรแกรมกล้อง หรือพิมพ์โค้ดด้านล่างใน terminal ก็ได้ เปิดได้เหมือนกัน

python infer_cam.py